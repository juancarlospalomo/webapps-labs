$(document).ready(function () {

    $('#labelTagISO').html(string_resource.ISO);
    $('#labelDate').html(string_resource.textDate);
    $('#labelNumber').html(string_resource.textNumber);
    $('#buttonClick').val(string_resource.textButton);

});